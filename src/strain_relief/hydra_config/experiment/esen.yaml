# @package _global_

defaults:
  - override /minimisation@local_min: esen
  - override /minimisation@global_min: esen
  - override /model: esen

conformers:
  numConfs: 20

model:
  model_paths: models/omol25_esen_sm_conserving

local_min:
  model_paths: models/omol25_esen_sm_conserving
  fmax: 0.50

global_min:
  model_paths: models/omol25_esen_sm_conserving
