# @package _global_

defaults:
  - override /minimisation@local_min: mace
  - override /minimisation@global_min: mace
  - override /model: mace

conformers:
  numConfs: 50

model:
  model_paths: models/MACE.model

local_min:
  model_paths: models/MACE.model
  fmax: 0.50

global_min:
  model_paths: models/MACE.model
